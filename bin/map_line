#!/usr/bin/python

import shlex
import sys
import subprocess

com = shlex.split(sys.argv[1])
for line in sys.stdin:
    args = shlex.split(line)
    with open('test.txt', 'a') as f:
        f.write(str(com + args))
    res = subprocess.check_output(com + args)
    print(res)

